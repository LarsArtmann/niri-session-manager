# Features

> Honest inventory of what this project does, by status. Generated by studying
> the actual code, not the marketing claims. Updated as features ship, change,
> or break.

## Status legend

| Status                    | Meaning                                                      |
| ------------------------- | ------------------------------------------------------------ |
| 🟢 `FULLY_FUNCTIONAL`     | Works as intended, exercised by tests or daily use.          |
| 🟡 `PARTIALLY_FUNCTIONAL` | Ships but has known gaps, edge-case bugs, or missing pieces. |
| 🔴 `BROKEN`               | Present in code but not working / disabled / failing.        |
| ⚪ `PLANNED`              | Designed or documented but **not yet implemented** in code.  |

> A feature earns `FULLY_FUNCTIONAL` only when you can point to the code that
> delivers it AND confirm it works. If you're unsure, it's `PARTIALLY_FUNCTIONAL`
> at best, never round up.

## Session capture and persistence

| Feature                                         | Status                    | Notes                                                                                                             |
| ----------------------------------------------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| Periodic session saving                         | 🟢 `FULLY_FUNCTIONAL`     | `src/main.rs:896` `periodic_save_session`; niri IPC `Windows`+`Workspaces` → JSON                                 |
| Atomic writes (temp + fsync + rename)           | 🟡 `PARTIALLY_FUNCTIONAL` | `src/main.rs:517` `atomic_write`; parent directory is not fsynced — rename may not survive power loss (TODO_LIST) |
| Backup rotation with retention                  | 🟢 `FULLY_FUNCTIONAL`     | `src/main.rs:931` `create_backup`; skips corrupt files since 0.4.0; cleanup path untested                         |
| Corrupt-session recovery via `.bak` fallback    | 🟢 `FULLY_FUNCTIONAL`     | `src/main.rs:957` `find_latest_valid_backup`; 3 dedicated tests                                                   |
| Boot-scoped restore gate (one restore per boot) | 🟢 `FULLY_FUNCTIONAL`     | `src/main.rs:65` `get_boot_id`, `src/main.rs:79` `already_restored_this_boot`; unit-tested                        |
| Dry-run preview                                 | 🟢 `FULLY_FUNCTIONAL`     | no spawning, no saving, no marker since 0.4.0 fixes; output format untested                                       |

## Restore

| Feature                                   | Status                    | Notes                                                                                                                                                       |
| ----------------------------------------- | ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Automatic restore on startup with retries | 🟡 `PARTIALLY_FUNCTIONAL` | `src/main.rs:633` `restore_session_internal`; works in daily use, but not idempotent — a partial failure re-spawns windows on retry; all IPC paths untested |
| Rate-limited spawning (max 5 concurrent)  | 🟡 `PARTIALLY_FUNCTIONAL` | `src/main.rs:745` global `Semaphore`; same-app concurrent spawns can swap workspaces (no window id from `Action::Spawn`)                                    |
| Count-based idempotent restore            | ⚪ `PLANNED`              | spawn `saved − running` per app; semantics pending (ROADMAP Q2)                                                                                             |
| Focus restoration                         | ⚪ `PLANNED`              | `SavedWindow.is_focused` saved but never read on restore                                                                                                    |
| `--max-restore-windows` cap               | 🟢 `FULLY_FUNCTIONAL`     | `src/main.rs:1052`, default 100, validated ≥ 1                                                                                                              |

## Terminal state recovery (Linux-only)

| Feature                                                          | Status                    | Notes                                                                                                                                          |
| ---------------------------------------------------------------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `/proc` process-tree capture (cmdline + cwd of foreground child) | 🟢 `FULLY_FUNCTIONAL`     | `src/proc.rs`; injectable `base: &Path`, fake-proc test convention                                                                             |
| Terminal-specific restore commands                               | 🟡 `PARTIALLY_FUNCTIONAL` | `src/main.rs:432` `build_terminal_restore_command` (kitty, foot, wezterm, ghostty, alacritty); flags never verified against real terminal CLIs |
| Flatpak/mapped-terminal profiles                                 | 🟢 `FULLY_FUNCTIONAL`     | `src/main.rs:401` `TerminalProfile::from_args`; reverse-DNS app-id matching, +2 tests                                                          |

## Configuration and integration

| Feature                                                                     | Status                    | Notes                                                                                                                                                   |
| --------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TOML config (app_mappings, single_instance_apps, skip_apps, terminal_state) | 🟢 `FULLY_FUNCTIONAL`     | `$XDG_CONFIG_HOME/niri-session-manager/config.toml`; invalid TOML warns and falls back to defaults; 4 parsing tests                                     |
| CLI with zero-value validation                                              | 🟢 `FULLY_FUNCTIONAL`     | `Config` struct `src/main.rs:1032`; `--save-interval/--max-backup-count/--spawn-timeout/--retry-attempts/--retry-delay/--max-restore-windows/--dry-run` |
| NixOS module                                                                | 🟡 `PARTIALLY_FUNCTIONAL` | `module.nix` mirrors 5 of 7 CLI flags (`maxRestoreWindows` and `dryRun` missing); systemd user service ordered after `niri.service`                     |
| Structured logging (tracing, `RUST_LOG`)                                    | 🟢 `FULLY_FUNCTIONAL`     | journald-native output with timestamps and levels                                                                                                       |
| Reactive saves via niri event stream                                        | ⚪ `PLANNED`              | polling only today; both status reports rank this High                                                                                                  |

---

_Verified against code 2026-09-03 (63/63 tests passing, `915216c`)._
